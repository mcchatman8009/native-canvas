{
  "name": "native-canvas",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint -p . -c tslint.json 'src/**/*.ts'",
    "libs": "mkdir -p dist/engine/sdl/sdl-shim; cp -r src/engine/sdl/sdl-shim dist/engine/sdl",
    "build": "rm -rf dist && mkdir -p dist/engine/sdl/sdl-shim && cp -r src/engine/sdl/sdl-shim dist/engine/sdl && tsc",
    "compile": "tsc",
    "clean": "rm -rf dist package-lock.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/ffi": "^0.2.1",
    "@types/node": "^10.12.2",
    "@types/ref": "0.0.28",
    "@types/ref-array": "0.0.28",
    "@types/ref-union": "0.0.28",
    "tslint": "^5.11.0",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "@rgba-image/png": "^0.1.0",
    "canvas": "^2.1.0",
    "color-rgba": "^2.1.0",
    "ffi": "github:node-ffi/node-ffi#169773d",
    "pngjs": "^3.3.3",
    "ref-array": "^1.2.0",
    "ref-union": "^1.0.1"
  }
}
